#include "main.h"
#include <string.h>

/**
 * _strcpy - copes a string
 * @src: pointer
 * @dest: another pointer
 * Return: the pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest , src);
	return (dest);
}
		
